# Tested with Python 3.12. Versions below are those used for the published analysis;
# the code is not sensitive to exact patch versions, so >= constraints are given.
# PyTorch CPU is sufficient; Intel Extension for PyTorch (IPEX) is optional acceleration.
numpy>=1.24
scipy>=1.10
pandas>=2.0
xarray>=2023.1
netCDF4>=1.6
matplotlib>=3.7
statsmodels>=0.14
utide>=0.3.1
scikit-learn>=1.3
torch>=2.0
